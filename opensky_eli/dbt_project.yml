name: opensky_eli

profile: opensky_eli

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  opensky_eli:
    +schema: raw

models:
  opensky_eli: 
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
